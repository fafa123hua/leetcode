### 代码

```js
var maximumWealth = function(accounts) {
    let ans = -1
    let r = 0
    for(let i in accounts){
        r = 0
        for(let j in accounts[i]){
            r += accounts[i][j]
        }
        ans = Math.max(ans, r)
    }
    return ans
};
```

