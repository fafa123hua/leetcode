### 代码

```js
var hasCycle = function(head) {
    let f = head
    let s = head
    while(f && f.next){
        f = f.next.next
        s = s.next
        if(f == s){
            return true
        }
    }
    return false
};
```

