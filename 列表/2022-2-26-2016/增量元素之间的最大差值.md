### 代码

```js
var maximumDifference = function(nums) {
    let ans = -1
    let pda = nums[0]
    for(let i = 1; i < nums.length; i++){
        if(nums[i] > pda){
            ans = Math.max(ans, nums[i] - pda)
        }else{
            pda = nums[i]
        }
    }
    return ans
};
```

