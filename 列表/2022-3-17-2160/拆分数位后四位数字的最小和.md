### 代码

```js
var minimumSum = function(num) {
    let ans = []
    while(num){
        ans.push(num % 10)
        num = Math.floor(num / 10)
    }
    ans.sort()
    return (ans[0] + ans[1]) * 10 + ans[2] + ans[3]
};
```

