### 代码

```js
var numJewelsInStones = function(jewels, stones) {
    let ans = 0
    let hash = new Map()
    for(let i of jewels){
        hash.set(i, 1)
    }
    for(let i of stones){
        if(hash.has(i)){
            ans++
        }
    }
    return ans
};
```

