### 代码

简单

```js
var maxPower = function(s) {
    let ans = 1
    let x = 1
    for(let i=0;i<s.length-1; i++){
        if(s[i] == s[i+1]){
            x++
        }
        ans = Math.max(ans,x)
        if(s[i] != s[i+1]){
            x = 1
        }
    }
    return ans
};
```

