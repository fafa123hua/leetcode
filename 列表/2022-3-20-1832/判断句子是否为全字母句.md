### 代码

```js
var checkIfPangram = function(sentence) {
    let hash = new Array(26).fill(0)
    for(let i = 0; i < sentence.length; i++){
        hash[sentence[i].charCodeAt(0) - 97] = 1
    }
    let ans = 0
    for(let i in hash){
        ans += hash[i]
    }
    return ans == 26
};
```

