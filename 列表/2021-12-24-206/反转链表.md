### 代码

```js
var reverseList = function(head) {
    let sen = null
    let n = head
    while(n){
        let next = n.next
        n.next = sen
        sen = n
        n = next
    }
    return sen
};
```

