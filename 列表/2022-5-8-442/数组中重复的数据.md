### 代码

```js
var findDuplicates = function(nums) {
    let ans = []
    for(let i = 0; i < nums.length; i++){
        let x = Math.abs(nums[i])
        if(nums[x - 1] > 0){
            nums[x - 1] *= -1
        }else{
            ans.push(x)
        }
    }
    return ans
};
```

