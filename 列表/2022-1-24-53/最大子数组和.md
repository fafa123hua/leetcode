### 代码

```js
var maxSubArray = function(nums) {
    let par = 0
    let ans = nums[0]
    for(let i of nums){
        par = Math.max(par + i, i)
        ans = Math.max(ans, par)
    }
    return ans
};
```

