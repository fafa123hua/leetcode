### 代码

```js
var targetIndices = function(nums, target) {
    nums.sort((a, b) => a - b)
    let ans = []
    for(let i in nums){
        if(nums[i] == target){
            ans.push(i)
        }
    }
    return ans
};
```

