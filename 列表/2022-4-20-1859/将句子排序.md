### 代码

```js
var sortSentence = function(s) {
    let str = s.split(' ')
    let ans = new Array(str.length)
    for(let i of str){
        ans[i[i.length - 1].charCodeAt(0) - '0'.charCodeAt(0) - 1] = i.substr(0, i.length - 1)
    }
    return ans.join(' ')
};
```

