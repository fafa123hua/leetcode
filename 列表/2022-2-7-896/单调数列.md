### 代码

```js
var isMonotonic = function(nums) {
    let l = true, d = true
    for(let i = 0; i < nums.length; i++){
        if(nums[i] > nums[i + 1]){
            d = false
        }
        if(nums[i] < nums[i + 1]){
            l = false
        }
    }
    return l || d
};
```

