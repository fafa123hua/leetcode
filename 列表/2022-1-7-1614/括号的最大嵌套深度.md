### 代码

```js
var maxDepth = function(s) {
    let ans = 0
    let x = 0
    for(let i of s){
        if(i == '('){
            x++
            ans = Math.max(ans, x)
        }
        if(i == ')'){
            x--
        }
    }
    return ans 
};
```

