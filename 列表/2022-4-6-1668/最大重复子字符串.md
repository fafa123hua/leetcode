### 代码

```js
var maxRepeating = function (sequence, word) {
  let max = 1
  let maxStr = word
  while (sequence.indexOf(maxStr) !== -1) {
    max++
    maxStr = word.repeat(max)
  }
  return max - 1
};
```

