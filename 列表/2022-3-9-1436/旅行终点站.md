### 代码

```js
var destCity = function(paths) {
    let hash = new Map()
    for(let i of paths){
        hash.set(i[0], 1)
    }
    for(let i of paths){
        if(!hash.has(i[1])){
            return i[1]
        }
    }
};
```

