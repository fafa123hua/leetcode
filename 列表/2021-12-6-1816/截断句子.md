### 代码

简单题

```js
var truncateSentence = function(s, k) {
    let ans = new Array()
    for(let i = 0; i < s.length; i++){
        if(s[i] == ' '){
            k--
        }
        if(k == 0){
            break
        }
        ans.push(s[i])
    }
    return ans.join('')
};
```

